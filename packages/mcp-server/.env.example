# Required: PostgreSQL connection string (same database as backend)
DATABASE_URL=postgresql://credentials:credentials_dev_password@localhost:5433/credentials

# Required: Email of the user operating this MCP session
MCP_USER_EMAIL=admin@lanyardhealth.com

# Optional: Log level (default: info)
LOG_LEVEL=info
